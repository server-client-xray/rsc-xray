{
  "name": "server-client-xray-oss",
  "private": true,
  "packageManager": "pnpm@9",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r eslint ."
  }
}
