{
  "name": "server-client-xray-oss",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r eslint ."
  },
  "devDependencies": {
    "@types/node": "20.15.0",
    "typescript": "5.5.4",
    "vitest": "1.5.2"
  }
}
